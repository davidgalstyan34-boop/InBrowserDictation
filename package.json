{
  "name": "in-browser-dictation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shortcut-driven Chrome dictation extension.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
